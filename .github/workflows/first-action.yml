name: First Workflow
on: 
# workflow_dispatch
  push
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print greeting
        run: echo "Hello world"
      - name: print goodbye
        run: echo "Done - bye"
